name: zebra_datawedge_example
description: Example app for zebra_wedge_scanner plugin
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  zebra_wedge_scanner:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
